name: Fetch Secret
description: Fetch a single named secret from the Oboist Secrets Store endpoint.

inputs:
  name:
    description: Name of the secret to fetch (must be registered in Oboist)
    required: true
  proxy_url:
    description: Oboist base URL
    required: true
  proxy_token:
    description: Oboist bearer token
    required: true

outputs:
  value:
    description: The fetched secret value
    value: ${{ steps.fetch.outputs.value }}

runs:
  using: composite
  steps:
    - name: Fetch secret
      id: fetch
      shell: bash
      env:
        PROXY_TOKEN: ${{ inputs.proxy_token }}
        PROXY_URL: ${{ inputs.proxy_url }}
      run: |
        RESPONSE=$(curl \
          --silent \
          --show-error \
          --fail-with-body \
          --request GET \
          "${PROXY_URL%/}/pit-boss/token?kind=secret&name=${{ inputs.name }}" \
          --header "Authorization: Bearer ${PROXY_TOKEN}")

        VALUE=$(printf '%s' "$RESPONSE" | jq -er '.value')
        echo "::add-mask::$VALUE"
        echo "value=$VALUE" >> "$GITHUB_OUTPUT"
